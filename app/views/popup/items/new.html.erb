<%= form_for [:popup, @item] do |f| %>
  
  <%= select_tag 'channel_id', options_for_select(@channels) %>
    
  <%= hidden_field_tag '[item][body]', @url %>
  <%= submit_tag 'Propagate' %>
  

<%- end -%>